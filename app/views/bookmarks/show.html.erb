<div class="container">
  <h1>Bookmarks</h1>
    <%= render partial: 'likes/like', locals: { bookmark: @bookmark, user_id: current_user.id } %>
    <%= link_to "#{@bookmark.url}", @bookmark.url %>
</div>